"use client";

/**
 * Schedule base styles live in `src/app/globals/globals-jp-lesson-schedule.css`
 * (imported via globals.css). Kept as a no-op so call sites stay stable.
 * Do not put page layout CSS back into styled-jsx here — App Router client
 * injection is unreliable for this route (unstyled /jp-lesson/schedule).
 */
export function JpLessonSchedulePageStyles() {
  return null;
}
