# 复制为 .env.deploy.local（已在 .gitignore，不会提交）
#
# 用于 git 自动提交后执行 npm run deploy（cron / 守护进程无交互环境）。
# 二选一即可：
#   A) 本机先执行一次：npx wrangler login
#   B) 推荐：在 Cloudflare 创建 API Token 并填到下面
#
# Dashboard → My Profile → API Tokens → Create Token
# 模板建议：Edit Cloudflare Workers（含 Workers Scripts、Routes、D1）
CLOUDFLARE_API_TOKEN=

# 一般可不填；多账号时从 Dashboard 右侧栏复制 Account ID
# CLOUDFLARE_ACCOUNT_ID=

# Bark（本机推送）—— 密钥放系统共享文件，勿写进 Git
#   系统文件：~/.config/bark/env
#   模板：    ~/.config/bark/env.example
#   优先级：进程环境 > 本文件（项目覆盖）> ~/.config/bark/env（跨项目默认）
# 其它项目只要读 ~/.config/bark/env 即可共用同一 key
# 线上 Worker 另走 wrangler secret（BARK_DEVICE_KEY），与本机文件无关
#
# 仅当本项目要覆盖系统默认时，再取消注释：
# BARK_DEVICE_KEY=
# BARK_PUSH_URL=https://api.day.app/<你的key>
# BARK_ENABLED=0
# BARK_NOTIFY_SUCCESS=0
# BARK_SOUND_DEPLOY_OK=bell
# BARK_ICON_DEPLOY_FAIL=https://finance.info-quests.com/bark/deploy-fail.png
# BARK_ICON_DEPLOY_OK=https://finance.info-quests.com/bark/deploy-ok.png
# BARK_ICON_CLASS_REMIND=https://finance.info-quests.com/bark/class-remind.png

# Mac 桌面通知（可选，默认开）：与 Bark 并行，部署成功/失败弹系统通知中心
# 临时关闭全部 Mac 通知：MAC_NOTIFY_ENABLED=0
# 只关「部署成功」Mac 通知（失败仍弹）：MAC_NOTIFY_SUCCESS=0
# 成功提示音（系统音名，默认 Glass）：MAC_SOUND_DEPLOY_OK=Glass
